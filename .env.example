# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/recon_agent.log

# Server Configuration
PORT=8000

# Optional shared secret. When set, callers (Power Automate) must send it
# as the X-API-Key header. Leave blank for local-only testing.
RECON_API_KEY=
